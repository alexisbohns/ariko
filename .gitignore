node_modules/
/.next
/out
*.tsbuildinfo
next-env.d.ts
.DS_Store
.env.local

# Visual brainstorming companion (local mockups, not source)
.superpowers/
.vercel
.env*
.worktrees/

# scratch probes from agent verification runs — never committed
__probe*

# The Profane display face is licensed, not open — serving it from our own
# CDN is permitted, publishing a copy in this PUBLIC repo is not (EULA §7).
# See app/fonts.ts.
/app/fonts/Profane-*
